"use client";
import {useEffect,useState} from "react";import Link from "next/link";
type Item={id:string;fileName:string;status:string;rowsImported:number;rowsSkipped:number;createdAt:string};
export default function History(){const[items,setItems]=useState<Item[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/import/history").then(r=>r.json()).then(d=>setItems(d.imports||[])).finally(()=>setLoading(false))},[]);return <main dir="rtl" className="min-h-screen bg-slate-50 p-4 md:p-8"><div className="mx-auto max-w-4xl"><Link href="/import" className="text-sm text-slate-500">← ייבוא חדש</Link><h1 className="mt-4 text-3xl font-bold">היסטוריית ייבואים</h1><p className="mt-2 text-slate-500">קבצי Excel שעיבדת במערכת</p>{loading?<div className="mt-8 rounded-2xl bg-white p-8 text-center">טוען…</div>:items.length===0?<div className="mt-8 rounded-2xl bg-white p-10 text-center"><div className="text-4xl">📂</div><h2 className="mt-3 text-xl font-semibold">עדיין אין ייבואים</h2><Link href="/import" className="mt-5 inline-block rounded-xl bg-slate-900 px-5 py-3 text-white">ייבוא ראשון</Link></div>:<div className="mt-8 space-y-3">{items.map(i=><div key={i.id} className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><b>{i.fileName}</b><div className="mt-1 text-sm text-slate-500">{new Date(i.createdAt).toLocaleString("he-IL")}</div></div><span className="rounded-full bg-emerald-100 px-3 py-1 text-xs text-emerald-700">{i.status==="COMPLETED"?"הושלם":i.status==="PROCESSING"?"בעיבוד":"נכשל"}</span></div>{i.status==="COMPLETED"&&<div className="mt-4 text-sm text-slate-600">{i.rowsImported} תנועות יובאו • {i.rowsSkipped} שורות דולגו</div>}</div>)}</div>}</div></main>}
