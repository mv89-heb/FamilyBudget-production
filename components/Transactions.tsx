"use client";

import { useEffect, useState } from "react";

type Cat={id:string;name:string;type:"INCOME"|"EXPENSE"};
type Method={id:string;nickname:string;last4:string|null;institution:string|null};
type Tx={id:string;type:"INCOME"|"EXPENSE";amount:number|string;transactionDate:string;category:{name:string};paymentMethod:Method|null;note:string|null;categoryId:string;paymentMethodId:string|null};
const initial=()=>({type:"EXPENSE",amount:"",transactionDate:new Date().toISOString().slice(0,10),categoryId:"",paymentMethodId:"",note:""});
const currentMonth=()=>new Date().toISOString().slice(0,7);
export default function Transactions(){
 const [rows,setRows]=useState<Tx[]>([]),[cats,setCats]=useState<Cat[]>([]),[methods,setMethods]=useState<Method[]>([]),[month,setMonth]=useState(currentMonth),[form,setForm]=useState<any>(initial()),[editing,setEditing]=useState<string|null>(null),[error,setError]=useState(""),[busy,setBusy]=useState(false);
 async function load(){const [a,b,c]=await Promise.all([fetch(`/api/transactions?month=${month}`),fetch("/api/categories"),fetch("/api/payment-methods")]);if(!a.ok||!b.ok||!c.ok)throw new Error("לא ניתן לטעון נתונים");setRows(await a.json());setCats(await b.json());setMethods(await c.json())}
 useEffect(()=>{load().catch(e=>setError(e.message))},[month]);
 const filteredCats=cats.filter(c=>c.type===form.type);
 async function save(e:React.FormEvent){e.preventDefault();if(busy)return;setError("");setBusy(true);try{const url=editing?`/api/transactions/${editing}`:"/api/transactions";const res=await fetch(url,{method:editing?"PUT":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await res.json();if(!res.ok)throw new Error(d.error||"שגיאה");setEditing(null);setForm(initial());await load()}catch(e){setError(e instanceof Error?e.message:"שגיאה")}finally{setBusy(false)}}
 async function remove(id:string){if(!confirm("למחוק את התנועה?"))return;const res=await fetch(`/api/transactions/${id}`,{method:"DELETE"});if(!res.ok)setError("לא ניתן למחוק");else await load()}
 function edit(r:Tx){setEditing(r.id);setForm({type:r.type,amount:Number(r.amount),transactionDate:r.transactionDate.slice(0,10),categoryId:r.categoryId,paymentMethodId:r.paymentMethodId||"",note:r.note||""})}
 return <div><div className="flex flex-wrap justify-between gap-3 mb-6"><div><h1 className="text-3xl font-black">תנועות</h1><p className="text-gray-500">ניהול הכנסות והוצאות</p></div><div className="flex items-center gap-2"><label htmlFor="transaction-period" className="text-sm text-gray-500">תקופה</label><select id="transaction-period" value={month} onChange={e=>setMonth(e.target.value)} className="border rounded-xl p-2 bg-white"><option value={currentMonth()}>החודש הנוכחי</option><option value="all">כל התקופות</option></select>{month!=="all"&&<input aria-label="בחירת חודש" type="month" value={month} onChange={e=>setMonth(e.target.value)} className="border rounded-xl p-2 bg-white"/>}</div></div><form onSubmit={save} className="bg-white rounded-2xl border p-5 mb-6 grid md:grid-cols-6 gap-3"><select value={form.type} onChange={e=>setForm({...form,type:e.target.value,categoryId:""})} className="border rounded-xl p-3"><option value="EXPENSE">הוצאה</option><option value="INCOME">הכנסה</option></select><input required type="number" step="0.01" min="0.01" max="999999999" placeholder="סכום" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} className="border rounded-xl p-3"/><input required type="date" value={form.transactionDate} onChange={e=>setForm({...form,transactionDate:e.target.value})} className="border rounded-xl p-3"/><select required value={form.categoryId} onChange={e=>setForm({...form,categoryId:e.target.value})} className="border rounded-xl p-3"><option value="">קטגוריה</option>{filteredCats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select value={form.paymentMethodId} onChange={e=>setForm({...form,paymentMethodId:e.target.value})} className="border rounded-xl p-3"><option value="">אמצעי תשלום</option>{methods.map(m=><option key={m.id} value={m.id}>{m.nickname}{m.last4?` •••• ${m.last4}`:""}</option>)}</select><input maxLength={500} placeholder="הערה בטוחה (אופציונלי)" value={form.note} onChange={e=>setForm({...form,note:e.target.value})} className="border rounded-xl p-3"/><div className="md:col-span-6 flex gap-2"><button disabled={busy} className="rounded-xl bg-gray-900 text-white px-5 py-3 disabled:opacity-50">{busy?"שומר...":editing?"שמירת שינוי":"הוספת תנועה"}</button>{editing&&<button type="button" onClick={()=>{setEditing(null);setForm(initial())}} className="rounded-xl border px-5">ביטול</button>}</div>{error&&<div role="alert" className="md:col-span-6 text-red-600">{error}</div>}</form><div className="bg-white rounded-2xl border overflow-auto"><table className="w-full text-sm"><thead className="bg-gray-50"><tr><th className="p-4 text-right">תאריך</th><th className="p-4 text-right">קטגוריה</th><th className="p-4 text-right">אמצעי</th><th className="p-4 text-right">סכום</th><th className="p-4 text-right">פעולות</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t"><td className="p-4">{new Date(r.transactionDate).toLocaleDateString("he-IL")}</td><td className="p-4">{r.category.name}</td><td className="p-4">{r.paymentMethod?`${r.paymentMethod.nickname}${r.paymentMethod.last4?` •••• ${r.paymentMethod.last4}`:""}`:"—"}</td><td className={`p-4 font-bold ${r.type==="EXPENSE"?"text-red-600":"text-green-600"}`}>{r.type==="EXPENSE"?"-":"+"}{Number(r.amount).toLocaleString("he-IL",{style:"currency",currency:"ILS"})}</td><td className="p-4"><button onClick={()=>edit(r)} className="ml-3">עריכה</button><button onClick={()=>remove(r.id)} className="text-red-600">מחיקה</button></td></tr>)}</tbody></table>{rows.length===0&&<p className="p-8 text-center text-gray-500">אין תנועות בתקופה זו.</p>}</div></div>
}
