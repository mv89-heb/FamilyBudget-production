# Shared financial UX

These components are presentation-only. Accounting truth remains in LedgerEngine and FinancialSourceOfTruth.

Use `MoneyValue` and `financialValue()` whenever a financial value can be unknown or partial. Never substitute zero for missing data.
