# Reconciliation safety policy

Financial records are never deleted or merged solely because their date, amount, note, or merchant matches another record.

A legacy transaction may receive a deterministic fingerprint automatically only when its identity is unique within the user's ledger. Any identity collision is reported for manual review.
